terraform {
  backend "s3" {
    bucket         = "nom-de-votre-bucket-terraform-state"
    key            = "dev/terraform.tfstate"
    region         = "eu-west-3"
    encrypt        = true
    dynamodb_table = "terraform-lock-table"
  }
}

terraform {
  backend "local" {}
}
